name: Deploy to IONOS Preprod

on:
  push:
    branches:
      - preprod  # Déploiement uniquement sur preprod

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout du code
      uses: actions/checkout@v3

    - name: Déployer avec SSH
      uses: appleboy/ssh-action@master
      with:
        host: access894124239.webspace-data.io
        username: u106767242
        key: ${{ secrets.REPIT_SSH_PRIVATE_KEY }}
        script: |
          cd /kunden/homepages/46/d894124239/htdocs/IllLovingDrivers
          git pull origin preprod
